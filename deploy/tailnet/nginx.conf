server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location = /.well-known/mdbase-app.json {
    default_type application/json;
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
